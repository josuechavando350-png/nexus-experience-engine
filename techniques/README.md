# Techniques

Validated, reusable Genome entries belong here as one JSON document per technique. A record is admitted only after review; fixtures remain under `tests/fixtures` so they cannot be mistaken for accepted knowledge.

